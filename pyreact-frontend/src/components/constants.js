// Here I'm declaring and exporting a constant that represents the base URL for the API.
// This makes it easier to manage and change if necessary, since it's used in several places throughout the application.
export const API_URL = "http://localhost:5000";
